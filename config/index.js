export const colors = {
  activeTab: '#00f',
  background: '#eee',
  primary: '#0f0',
  secondary: '#f00',
  text: '#333'
};

Object.assign(colors, {
  record: colors.primary,
  report: colors.secondary
});

export const fonts = {
  heading: 'Al Nile'
}
